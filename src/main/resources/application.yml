spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: false

  h2:
    console:
      enabled: true
      path: /h2-console